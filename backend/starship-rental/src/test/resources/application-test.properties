spring.application.name=starship-rental-test

# Testcontainers will provide the DataSource automatically via @ServiceConnection
# These properties are kept for documentation purposes only

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.springframework.test=INFO
logging.level.com.starwars.starshiprental=DEBUG
